package fit.hutech.spring;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Application configuration beans can be added here
    // Data should be loaded from the database via repositories, not hard-coded
}